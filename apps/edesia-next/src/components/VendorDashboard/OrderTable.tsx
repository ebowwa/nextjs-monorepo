import { Table, TableHead, TableRow, TableHeader, TableCell, TableBody } from "@/components/ui/common/table";

const OrderTable = () => {
  return (
    <Table>
      <TableHeader>
        {/* Table Headers */}
      </TableHeader>
      <TableBody>
        {/* Table Rows */}
      </TableBody>
    </Table>
  );
};

export default OrderTable;
